using UnityEngine;
using UnityEngine.UI;

public class FarsightSymbolPreview : MonoBehaviour
{
    /*[SerializeField] private Image nextSymbolImage;
    
    [SerializeField] private Image nextSymbolAlternativeImage;

    // Set from your game logic (or via DI)
    public bool FarsightActive { get; set; }
    public Sprite nextSymbolSprite { get; set; }
    public Sprite nextSymbolAlternativeSprite { get; set; }
    
    private void Update()
    {
        if (FarsightActive)
        {
            if (!previewPanel.activeSelf)
                previewPanel.SetActive(true);

            nextSymbolImage.sprite = nextSymbolSprite;
            nextSymbolAlternativeImage.sprite = nextSymbolAlternativeSprite;
        }
        else
        {
            if (previewPanel.activeSelf)
                previewPanel.SetActive(false);
        }
    }*/
}